package hotelappfinal;
import java.time.LocalDate;
public abstract class User {
    
    private String userId;
    private String userJoiningDate;
    private String userName;
    private String userEmail;
    private String userPhone;
    private String userAddress;
    private String role;

    public User(String userId, String userJoiningDate, String userName, String userEmail, String userPhone, String userAddress, String role) {
        this.userId = userId;
        this.userJoiningDate = userJoiningDate;
        this.userName = userName;
        this.userEmail = userEmail;
        this.userPhone = userPhone;
        this.userAddress = userAddress;
        this.role = role;
    }
    

    public void setUserId(String userId) {
        if((userId!=null)&&(!userId.equals("")&&(isDigit(userId))))// To ensure user ID is vaild.
        this.userId = userId;
    }

    public void setUserJoiningDate(String userJoiningDate) {
        if((userJoiningDate!=null)&&(!userJoiningDate.equals("")))// To ensure that the date is vaild.
        this.userJoiningDate = userJoiningDate;
    }

    public void setUserName(String userName) {
        if((userName!=null)&&(!userName.equals("")))// To ensure user name is vaild.
        this.userName = userName;
    }

    public void setUserEmail(String userEmail) {
        if((userEmail!=null)&(!userEmail.equals(""))&&(userEmail.contains("@")))// To ensure user email is vaild. And it contains @.
        this.userEmail = userEmail;
    }

    public void setUserPhone(String userPhone) {
        if((userPhone!=null)&&(!userPhone.equals(""))&&isDigit(userPhone))// To ensure user number is vaild. And it is only contains numbers.
        this.userPhone = userPhone;
    }

    public void setUserAddress(String userAddress) {
        if((userAddress!=null)&&(!userAddress.equals("")))// To ensure user address is vaild.
        this.userAddress = userAddress;
    }

    public void setRole(String role) {
        this.role = role;
    }

    public String getUserId() {
        return userId;
    }

    public String getUserJoiningDate() {
        return userJoiningDate;
    }

    public String getUserName() {
        return userName;
    }

    public String getUserEmail() {
        return userEmail;
    }

    public String getUserPhone() {
        return userPhone;
    }

    public String getUserAddress() {
        return userAddress;
    }

    public String getRole() {
        return role;
    }
    
    
    
    public boolean isDigit(String value){// A method to cheack user number if it's only contains numbers.
        for(int i=0;i<value.length();i++){// Loop throug the number 
            if(!Character.isDigit(value.charAt(0)))
                return false;
        }
        return true;
    }
    
     @Override
    public String toString() { // displays user details.
        return "User ID: " + userId + "\nJoin Date: " + userJoiningDate + "\nName: " + userName + "\nEmail: " + userEmail + "\nPhone number: " + userPhone + "\nAdress: " + userAddress + "\nRole: " + role;
    }
    
    
    
}
