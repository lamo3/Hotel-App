package hotelappfinal;

import java.util.Scanner;


public class Service {
    
    Scanner input=new Scanner(System.in);
    private String serviceId;
    private String serviceName;
    private String description;
    private double price;
    private String reservationId;
    private String requestDate;
    private String serviceStatus;
    
    

    public Service(String serviceId, String serviceName, String description, double price, String reservationId, String requestDate, String serviceStatus) {
      setServiceId(serviceId);
       setServiceName(serviceName);
       setDescription(description);
       setPrice(price);
       setReservationId(reservationId);
        setRequestDate(requestDate);
       setServiceStatus(serviceStatus);
    }

    
    
    
    
    public void setServiceId(String serviceId) {
        if((serviceId!=null)&&(!serviceId.equals("")))// To ensure service ID is vaild.
        this.serviceId = serviceId;
    }

    public void setServiceName(String serviceName) {
        if((serviceName!=null)&&(!serviceName.equals("")))// To ensure the service name is vaild.
        this.serviceName = serviceName;
    }

    public void setDescription(String description) {
        if((description!=null)&&(!description.equals("")))// To ensure the description is vaild.
        this.description = description;
    }

    public void setPrice(double price) {
        if(price>0)// price must be grater than 0.
        this.price = price;
    }

    public void setReservationId(String reservationId) {
        if((reservationId!=null)&&(!reservationId.equals("")))// To ensure reservation ID is vaild.
        this.reservationId = reservationId;
    }

    public void setRequestDate(String requestDate) {
        if((requestDate!=null)&&(!requestDate.equals("")))// To ensure that the date is vaild.
        this.requestDate = requestDate;
    }

    public void setServiceStatus(String serviceStatus) {
        if((serviceStatus.equals("Requested"))||(serviceStatus.equals("In Progress"))||(serviceStatus.equals("Completed"))||(serviceStatus.equals("Cancelled")))// To ensure the entered service ststus is allowed.
        this.serviceStatus = serviceStatus;
    }

    public String getServiceId() {
        return serviceId;
    }

    public String getServiceName() {
        return serviceName;
    }

    public String getDescription() {
        return description;
    }

    public double getPrice() {
        return price;
    }

    public String getReservationId() {
        return reservationId;
    }

    public String getRequestDate() {
        return requestDate;
    }

    public String getServiceStatus() {
        return serviceStatus;
    }
    
    
    }
    
    
    
